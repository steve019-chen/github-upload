---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
      - /dev/sdc
    logical_volumes:
      app02lv: {fs_type: ext4, size: 500G, mountpath: /var/kafka}
      app01lv: {fs_type: ext4, size: 50G, mountpath: /work/infra}
