---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
      - /dev/sdc
    logical_volumes:
      app01lv: {fs_type: ext4, size: 50G, mountpath: /niceapps}
      app02lv: {fs_type: ext4, size: 450G, mountpath: /totalview}
