---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
      app02lv: {fs_type: ext4, size: 50G, mountpath: /apps}
      app01lv: {fs_type: ext4, size: 100G, mountpath: /data}
