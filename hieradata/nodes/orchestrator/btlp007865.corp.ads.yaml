---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
      - /dev/sdc
    logical_volumes:
      app01lv: {fs_type: ext4, size: 60G, mountpath: /niceapps}
      app02lv: {fs_type: ext4, size: 500G, mountpath: /totalview}
