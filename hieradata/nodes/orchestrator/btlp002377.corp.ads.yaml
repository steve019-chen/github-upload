---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
       app03lv: {fs_type: ext4, size: 50G, mountpath: /work}
       app01lv: {fs_type: ext4, size: 45G, mountpath: /data}
       app02lv: {fs_type: ext4, size: 50G, mountpath: /apps}