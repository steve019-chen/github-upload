---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
      app04lv: {fs_type: ext4, size: 45G, mountpath: /backup}
      app03lv: {fs_type: ext4, size: 30G, mountpath: /data}
      app01lv: {fs_type: ext4, size: 20G, mountpath: /opt/infra}
      app02lv: {fs_type: ext4, size: 10G, mountpath: /var/log/infra}
