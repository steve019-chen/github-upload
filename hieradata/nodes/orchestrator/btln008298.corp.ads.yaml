---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
      app02lv: {fs_type: ext4, size: 35G, mountpath: /fs01}
      app01lv: {fs_type: ext4, size: 10G, mountpath: /opt/tuxedo}
