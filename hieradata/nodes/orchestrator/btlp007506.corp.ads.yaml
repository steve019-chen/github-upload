---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
      - /dev/sdc
    logical_volumes:
      app02lv: {fs_type: ext4, size: 200G, mountpath: /apps/infra}
      app01lv: {fs_type: ext4, size: 300G, mountpath: /work/infra}
