---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
       app02lv: {fs_type: ext4, size: 2G, mountpath: /apps/bar}
       app01lv: {fs_type: ext4, size: 1G, mountpath: /apps/foo}