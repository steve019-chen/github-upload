---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
      app01lv: {fs_type: ext4, size: 100G, mountpath: /apps}
      app02lv: {fs_type: ext4, size: 50G, mountpath: /work}
