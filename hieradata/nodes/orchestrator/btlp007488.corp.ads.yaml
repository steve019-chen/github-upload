---
lvm::volume_groups:
  appvg:
    createonly: true
    physical_volumes:
      - /dev/sdb
    logical_volumes:
      app01lv: {fs_type: ext4, size: 100G, mountpath: /work/db}
      app02lv: {fs_type: ext4, size: 30G, mountpath: /work/infra/app}
